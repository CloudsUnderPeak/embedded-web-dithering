interface:
  display_name: "Staged Commit"
  short_description: "Commit staged changes with [Add]/[Fix] messages"
  default_prompt: "Use $staged-commit to inspect staged changes and create a focused commit."
